// THIS FILE IS DEPRECATED - ALL ACTIONS HAVE BEEN MOVED
// The logic has been moved to /lib/actions/server-actions.ts to ensure
// a strict separation between server and client code, which was causing
// the "Internal Server Error".

// We keep this file to avoid breaking imports that might still point here,
// but it should be considered for removal in a future cleanup.

export {};
